{% extends 'base.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
    <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ menu.libelle }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ menu.lien }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ menu.type }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ menu.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('objet_menu_index') }}">back to list</a>

    <a href="{{ path('objet_menu_edit', {'id': menu.id}) }}">edit</a>

    {{ include('objet_menu/_delete_form.html.twig') }}
{% endblock %}
