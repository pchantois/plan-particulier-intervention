{% extends 'base_' ~ ga_theme ~ '.html.twig' %}

{% block title %}ppi{% endblock %}

{% block body %}
<a href="{{ path('admin_operation_new') }}">Add Operation</a>
{# <article id="liste">
    <h2>PPI</h2>
    <table class="table-wrapper">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Libelle</th>
                <!-- <th>Description</th>
                <th>Commentaire</th> -->
            </tr>
        </thead>
        <tbody>
        {% for operation in operations %}
            <tr>
                <td><a href="{{ path('admin_operation_data_with_updateOperation', {'id': operation.id}) }}">{{ operation.id }}</a></td>
                <td><a href="{{ path('admin_operation_data_with_updateOperation', {'id': operation.id}) }}">{{ operation.code }}</a></td>
                <td><a href="{{ path('admin_operation_data_with_updateOperation', {'id': operation.id}) }}">{{ operation.libelle }}</a></td>
                <!-- <td>{{ operation.description }}</td>
                <td>{{ operation.commentaire }}</td> -->
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</article> #}
<article id="ppi">
    {# <h2>{{ configs.site.libelle.mainTitle }}</h2> #}
    {% include "admin/operation/ppi_global.html.twig" with {'items': ppi} %}
</article>

    {# {% include "admin/operation/ppi_global.html.twig" with {'items': ppi} %} #}
    {# {% include "admin/operation/ppi_parCategorie.html.twig" with {'items': ppi2} %} #}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{asset('assets/js/table-scroll.js')}}"></script>
    <script src="{{asset('assets/js/table-scroll.min.js')}}"></script>
    <script src="{{asset('assets/js/table-scroll-config.min.js')}}"></script>
{% endblock %}
